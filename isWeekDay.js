var arr = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];

function isWeekday(parameter){
  var week = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
  if (parameter.startsWith('S')) {
    return false;
  } return true;
}